<configuration>
    <conversionRule conversionWord="threadID" converterClass="com.grandp.data.log_helper.ThreadIdConverter" />
    <conversionRule conversionWord="user" converterClass="com.grandp.data.log_helper.UserPrincipalAccessor" />


    <property name="logPattern" value="[%date{YYYY-MM-dd}] | [%date{HH:mm:ss.SSS Z}] | [%level] | Thread-[%threadID] | User-[%user] | [%logger]%n-TRACE-: %msg%n%n" />

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
    </appender>

    <appender name="file" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>application%i.log</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>5</maxIndex>
        </rollingPolicy>

        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>10MB</maxFileSize>
        </triggeringPolicy>

        <encoder>
            <pattern>${logPattern}</pattern>
        </encoder>
    </appender>

    <include resource="logback_tracing_config.xml"/>

    <root level="WARN">
        <appender-ref ref="console" />
        <appender-ref ref="file" />
    </root>
</configuration>